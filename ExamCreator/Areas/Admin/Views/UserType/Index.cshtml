@model IEnumerable<DTOLayer.DTOs.UserType.UserTypeIndexDTO>

@{
    ViewData["Title"] = "İstifadəçi tipi";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>İstifadəçi tipi</h1>


<p>
    <a asp-action="Create" class="btn btn-vimeo" title="Yeni məlumat" style="justify-content: center;">
        Yeni SİNİF
        <lord-icon src="https://cdn.lordicon.com/rfbqeber.json"
                   trigger="hover"
                   style="width:40px;height:40px">
        </lord-icon>
    </a>

</p>
<table class="table">
    <thead>
        <tr>
            <th>
                TİP
            </th>
            <th>
                STATUS
            </th>
            <th>
                SİLİNMİŞ
            </th>
            <th>
                DƏYİŞDİRİLDİ
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Type)
                </td>
                <td>
                    @if (item.Status)
                    {
                        <span class="badge badge-pill text-white bg-green">Aktiv</span>
                    }
                    else
                    {
                        <span class="badge badge-pill text-white bg-muted">Passiv</span>
                    }
                </td>
                <td>
                    @if (item.IsDeleted)
                    {
                        <span class="badge badge-pill text-white bg-red">Silinib</span>
                    }
                </td>
                <td>
                    @item.ModifyedDate.Value.ToString("dd.MM.yyyy - HH:mm")
                </td>

                <td>
                    @await Html.PartialAsync("~/Views/Shared/_ToolsButtonPartial.cshtml", new ToolsButtonViewModel{ Area="admin", Controller="UserType", RouteId = item.Id.ToString()})

                </td>
            </tr>
        }
    </tbody>
</table>

